.{
    .version = 1,
    .name = "farm-game",
    .description = "A farm game demonstrating labelle-tasks plugin",
    .initial_scene = "main",
    .backend = .raylib,
    .ecs_backend = .zig_ecs,
    .window = .{
        .width = 800,
        .height = 600,
        .title = "Farm Game",
        .target_fps = 60,
    },
    .resources = .{},
    .plugins = .{
        .{ .name = "labelle-tasks", .version = "0.2.0" },
    },
}
